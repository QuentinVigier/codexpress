<nav class="flex items-center justify-between p-2 bg-neutral-100">
	<a href="{{path('app_home')}}" class="flex items-center gap-1">
		<img src="{{ asset('img/logo_dark.svg') }}" alt="Avatar" width="32" class="transition-transform duration-200 ease-in-out rounded-full hover:scale-95">
		<p>CodeXpress</p>
	</a>
	<div class="flex items-center justify-between gap-4">
		<form action="" method="post" class="items-center hidden p-2 border-2 rounded-md md:flex border-neutral-700">
			<input type="hidden" name="csrf_token" value="{{csrf_token('authenticate')}}">
			<input type="text" name="" id="" placeholder="Search notes" class="bg-transparent">
			<button type="submit">{{ ux_icon('mdi:search',  { 'class': 'h-6 w-6'}) }}</button>
		</form>
		<div class="flex items-center justify-between gap-4">
			<a href="{{path('app_note_new')}}" class="p-2 border-2 rounded-md border-neutral-700">{{ ux_icon('mdi:plus-box-outline', { 'class': 'h-6 w-6'}) }}</a>
			<a href="{{path('app_profile_edit')}}" class="p-2 border-2 rounded-md border-neutral-700">{{ ux_icon('mdi:cog-outline', { 'class': 'h-6 w-6'}) }}</a>
			<a href="{{path('app_note_user', {'username': app.user.username ?? 'martin'})}}" class="p-2 rounded-full">
				<img src="https://avatar.iran.liara.run/public" alt="Avatar" width="32" class="">
			</a>
		</div>
	</div>
</nav>
